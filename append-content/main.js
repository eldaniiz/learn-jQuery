$(document).ready(function() {
    $("#container").append("It's New Paragraph After HTML Elements"); // it's append your text after html defined elements
    // $("#container").prepend("Smth") it's append your text before html defined elements
    // .before/.after adds your text before and after container class
    // we can add more than one text in .append method
    // also we can add variable into append method
});